config:
  clients:
    # Push THẲNG vào Loki gateway (collector đã bỏ loki receiver -> port 3500 refused).
    - url: http://loki-gateway/loki/api/v1/push
  snippets:
    pipelineStages:
      - docker: {}